function searchSite(e){e.preventDefault();const query=e.target.querySelector('input').value.trim();alert(query?`Searching ElimuPlus for “${query}”...`:'Enter a course, subject, teacher or book to search.')}function join(){alert('Welcome to ElimuPlus! Account registration will connect to Supabase in the production version.')}function choosePlan(){alert('Great choice. Payment setup (including M-Pesa) is ready for backend integration.')}
